import Stub from '../components/Stub.jsx'

export default function CustomList() {
  return (
    <Stub
      title="Custom List"
      summary="For the job that fits neither template — build the checklist you actually need."
      points={[
        'Assemble a list from your own test items',
        'Save it for reuse, or keep it to the one job',
        'Same PASS / FAIL / N/A and notes as the standard lists',
        'Exports through the same report builder',
      ]}
      step="its own planning step"
    />
  )
}
